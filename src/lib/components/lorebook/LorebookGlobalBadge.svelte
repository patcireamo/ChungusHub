<script lang="ts">
	/**
	 * The mark a book switched into every chat wears, wherever the shelf draws one: the list
	 * row, the grid tile and the gallery card. One component rather than a pill written out
	 * three times, so three surfaces cannot end up naming the same state three ways.
	 *
	 * It wears the accent the app marks a claimed state with, the library's own Default pill to
	 * the class, and carries the blur that pill needs where it lands over a picture.
	 */
</script>

<span
	class="shrink-0 text-[10px] font-ui px-1.5 py-0.5 rounded-full bg-accent/14 text-accent border border-accent/30 backdrop-blur-sm"
	title="In every chat, with no character or persona linking it"
>
	Every chat
</span>
